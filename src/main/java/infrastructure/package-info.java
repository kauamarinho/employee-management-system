/**
 * Reserved for future infrastructure concerns (external adapters, configuration,
 * file or network I/O). The application currently has no external dependencies:
 * data is kept in memory by {@link domain.repository.EmployeeRepository}.
 */
package infrastructure;
